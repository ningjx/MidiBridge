<Window x:Class="MidiBridge.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:MidiBridge.ViewModels"
        xmlns:converters="clr-namespace:MidiBridge.Converters"
        Title="MIDI Bridge"
        Background="#1A1A1A"
        Closing="Window_Closing"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResize">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0"
                      GlassFrameThickness="0"
                      CornerRadius="8"
                      ResizeBorderThickness="5"/>
    </WindowChrome.WindowChrome>
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVis"/>
        <converters:StatusToBrushConverter x:Key="StatusBrush"/>
        <converters:IsConnectedConverter x:Key="IsConnected"/>
        <converters:BoolToMarginConverter x:Key="BoolToMargin"/>
        <converters:BoolToWarningTagConverter x:Key="BoolToWarningTag"/>
        
        <SolidColorBrush x:Key="PanelBg" Color="#1A1A1A"/>
        <SolidColorBrush x:Key="HeaderBg" Color="#252525"/>
        <SolidColorBrush x:Key="ItemBg" Color="#2A2A2A"/>
        <SolidColorBrush x:Key="ConnectorNormal" Color="#4A90D9"/>
        <SolidColorBrush x:Key="ConnectorActive" Color="#5CB85C"/>
        
        <!-- 现代化右键菜单样式 -->
        <Style x:Key="ModernContextMenu" TargetType="ContextMenu">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="BorderBrush" Value="#3D3D3D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="4">
                            <ItemsPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ModernMenuItem" TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4A90D9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 现代化按钮样式 -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#3D3D3D"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4A4A4A"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#555555"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#333333"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 强调按钮样式（蓝色） -->
        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#4A90D9"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5A9FE8"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#3A80C9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- 危险按钮样式（红色） -->
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#C75050"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D76060"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#B74040"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- 成功按钮样式（绿色） -->
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#5CB85C"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#6CC86C"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#4CA84C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- 现代化输入框样式 -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3D3D3D"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          VerticalAlignment="Center"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#4A90D9"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#4A4A4A"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#252525"/>
                                <Setter Property="Foreground" Value="#888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 带警告的端口输入框样式 -->
        <Style x:Key="PortTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3D3D3D"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="2,0"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#4A90D9"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#4A4A4A"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="Warning">
                                <Setter TargetName="border" Property="BorderBrush" Value="#E74C3C"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="3"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 小型输入框样式 -->
        <Style x:Key="SmallTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3D3D3D"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="2,0"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Center"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#4A90D9"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#4A4A4A"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#252525"/>
                                <Setter Property="Foreground" Value="#888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 透明按钮样式 -->
        <Style x:Key="TransparentButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#333"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#555"/>
                                <Setter Property="Foreground" Value="#AAA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 标题栏按钮样式 -->
        <Style x:Key="TitleBarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#AAA"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 关闭按钮样式 -->
        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource TitleBarButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 自定义标题栏 -->
        <Border Grid.Row="0" Background="{StaticResource HeaderBg}" BorderBrush="#2A2A2A" BorderThickness="0,0,0,1"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid Height="32">
                <TextBlock Text="MIDI Bridge" Margin="12,0,0,0" 
                           VerticalAlignment="Center" Foreground="#E0E0E0" FontSize="12"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource TitleBarButton}" Click="MinimizeButton_Click">
                        <TextBlock Text="─" FontSize="16" Width="12" TextAlignment="Center"/>
                    </Button>
                    <Button Style="{StaticResource TitleBarButton}" Click="MaximizeButton_Click">
                        <TextBlock Text="▢" FontSize="14" Width="14" TextAlignment="Center"/>
                    </Button>
                    <Button Style="{StaticResource CloseButton}" Click="CloseButton_Click">
                        <TextBlock Text="✕" FontSize="14" Width="14" TextAlignment="Center"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 顶部工具栏 -->
        <Border Grid.Row="1" Background="{StaticResource HeaderBg}" Padding="10,6" BorderBrush="#2A2A2A" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 左侧：输入设备标题 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0,15,0">
                    <TextBlock Text="输入设备" FontWeight="SemiBold" Foreground="#CCC"/>
                    <TextBlock Text="{Binding InputDevices.Count, StringFormat=' ({0})'}" 
                               Foreground="#888"/>
                </StackPanel>

                <!-- 端口设置 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,15,0">
                    <TextBlock Text="RTP-MIDI:" VerticalAlignment="Center" Foreground="#AAA" Margin="0,0,6,0"/>
                    <TextBox x:Name="RtpPortTextBox" 
                             Text="{Binding RtpPort, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource PortTextBox}" 
                             Margin="0,0,3,0"
                             ToolTip="{Binding RtpPortWarningMessage}"
                             Tag="{Binding RtpPortWarning, Converter={StaticResource BoolToWarningTag}}"/>
                    <TextBlock Text="-" VerticalAlignment="Center" Foreground="#AAA" Margin="0,0,3,0"/>
                    <TextBox Text="{Binding RtpDataPort, Mode=OneWay}" Style="{StaticResource SmallTextBox}" Margin="0,0,15,0" IsReadOnly="True"/>
                    <TextBlock Text="NM2:" VerticalAlignment="Center" Foreground="#AAA" Margin="0,0,6,0"/>
                    <TextBox x:Name="NM2PortTextBox" 
                             Text="{Binding NM2Port, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource PortTextBox}"
                             ToolTip="{Binding NM2PortWarningMessage}"
                             Tag="{Binding NM2PortWarning, Converter={StaticResource BoolToWarningTag}}"/>
                </StackPanel>

                <!-- 中间填充 -->
                <Border Grid.Column="2"/>

                <!-- 按钮区域 -->
                <StackPanel Grid.Column="3" Orientation="Horizontal" Margin="15,0,15,0">
                    <Button Content="扫描本地设备" Command="{Binding ScanCommand}" Style="{StaticResource ModernButton}" Margin="0,0,10,0"/>
                    <Button Content="发现NM2设备" Command="{Binding RefreshNM2DiscoveryCommand}" Style="{StaticResource ModernButton}" Margin="0,0,10,0"
                            ToolTip="搜索 Network MIDI 2.0 设备"/>
                    <Button Content="{Binding NetworkButtonText}" 
                            Command="{Binding ToggleNetworkCommand}"
                            MinWidth="80">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ModernButton}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsNetworkRunning}" Value="True">
                                        <Setter Property="Background" Value="#C75050"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

                <!-- 右侧：输出设备标题 -->
                <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0,5,0">
                    <TextBlock Text="输出设备" FontWeight="SemiBold" Foreground="#CCC"/>
                    <TextBlock Text="{Binding OutputDevices.Count, StringFormat=' ({0})'}" 
                               Foreground="#888"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主连线区域 -->
        <Grid Grid.Row="2" Background="#1A1A1A">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="220"/>
            </Grid.ColumnDefinitions>

            <!-- 连线画布 - 放在设备列表下方 -->
            <Canvas Grid.Column="0" Grid.ColumnSpan="3" x:Name="ConnectionCanvas" 
                    Background="{x:Null}" Panel.ZIndex="0"/>
            
            <!-- 左侧：输入设备列表 -->
            <Grid Grid.Column="0">
                <ScrollViewer x:Name="InputScrollViewer" VerticalScrollBarVisibility="Hidden" ScrollChanged="ScrollViewer_ScrollChanged" Panel.ZIndex="1">
                    <ItemsControl ItemsSource="{Binding InputDevices}" x:Name="InputItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource ItemBg}" CornerRadius="6" Margin="3" Padding="12,10"
                                    BorderThickness="1"
                                    Tag="{Binding Id}" Cursor="Hand"
                                    MouseLeftButtonDown="Device_MouseLeftButtonDown"
                                    MouseMove="Device_MouseMove"
                                    MouseRightButtonDown="Device_RightClick">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="BorderBrush" Value="#4A90D9"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#323232"/>
                                            </Trigger>
                                            <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                                                <Setter Property="BorderBrush" Value="#3D3D3D"/>
                                                <Setter Property="Opacity" Value="0.5"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Border.ContextMenu>
                                    <ContextMenu Style="{StaticResource ModernContextMenu}" Opened="ContextMenu_Opened">
                                        <MenuItem Header="禁用" Style="{StaticResource ModernMenuItem}"/>
                                    </ContextMenu>
                                </Border.ContextMenu>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0">
                                        <TextBlock Text="{Binding Name, Mode=OneWay}" FontWeight="Medium" FontSize="12" 
                                                  Foreground="#E0E0E0" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock FontSize="10" Foreground="#777" Margin="0,3,0,0">
                                            <Run Text="{Binding TypeText, Mode=OneWay}"/>
                                        </TextBlock>
                                    </StackPanel>

                                    <!-- 连接点 -->
                                    <Ellipse Grid.Column="1" Width="18" Height="18"
                                              StrokeThickness="3" Cursor="Hand"
                                              VerticalAlignment="Center"
                                              ToolTip="点击选择，然后点击输出设备创建连线"
                                              MouseLeftButtonDown="InputConnector_Click">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Stroke" Value="#666666"/>
                                                <Setter Property="Fill" Value="#3A3A3A"/>
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding IsEnabled}" Value="True"/>
                                                            <Condition Binding="{Binding IsSelected}" Value="False"/>
                                                            <Condition Binding="{Binding IsTransmitting}" Value="False"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Stroke" Value="{StaticResource ConnectorNormal}"/>
                                                        <Setter Property="Fill" Value="#1A1A1A"/>
                                                    </MultiDataTrigger>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding IsEnabled}" Value="True"/>
                                                            <Condition Binding="{Binding IsSelected}" Value="True"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Stroke" Value="Orange"/>
                                                        <Setter Property="Fill" Value="Orange"/>
                                                    </MultiDataTrigger>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding IsEnabled}" Value="True"/>
                                                            <Condition Binding="{Binding IsTransmitting}" Value="True"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Stroke" Value="#5CB85C"/>
                                                        <Setter Property="Fill" Value="#5CB85C"/>
                                                        <Setter Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect Color="#5CB85C" BlurRadius="15" ShadowDepth="0" Opacity="1"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </MultiDataTrigger>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding IsEnabled}" Value="True"/>
                                                            <Condition Binding="{Binding HasActiveRoute}" Value="True"/>
                                                            <Condition Binding="{Binding IsSelected}" Value="False"/>
                                                            <Condition Binding="{Binding IsTransmitting}" Value="False"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Stroke" Value="{StaticResource ConnectorActive}"/>
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                </ScrollViewer>
                <Canvas x:Name="InputDropCanvas" Panel.ZIndex="0" IsHitTestVisible="False"/>
            </Grid>

            <!-- 右侧：输出设备列表 -->
            <Grid Grid.Column="2">
                <ScrollViewer x:Name="OutputScrollViewer" VerticalScrollBarVisibility="Hidden" ScrollChanged="ScrollViewer_ScrollChanged" Panel.ZIndex="1">
                <ItemsControl ItemsSource="{Binding OutputDevices}" x:Name="OutputItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource ItemBg}" CornerRadius="6" Margin="3" Padding="12,10"
                                    BorderThickness="1"
                                    Tag="{Binding Id}" Cursor="Hand"
                                    MouseLeftButtonDown="Device_MouseLeftButtonDown"
                                    MouseMove="Device_MouseMove"
                                    MouseRightButtonDown="Device_RightClick">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="BorderBrush" Value="#4A90D9"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#323232"/>
                                            </Trigger>
                                            <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                                                <Setter Property="BorderBrush" Value="#3D3D3D"/>
                                                <Setter Property="Opacity" Value="0.5"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Border.ContextMenu>
                                    <ContextMenu Style="{StaticResource ModernContextMenu}" Opened="ContextMenu_Opened">
                                        <MenuItem Header="禁用" Style="{StaticResource ModernMenuItem}"/>
                                    </ContextMenu>
                                </Border.ContextMenu>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- 连接点 -->
                                    <Ellipse Grid.Column="0" Width="18" Height="18"
                                             StrokeThickness="3" Cursor="Hand"
                                             VerticalAlignment="Center" Margin="0,0,10,0"
                                             ToolTip="点击选择作为连线目标"
                                             MouseLeftButtonDown="OutputConnector_Click">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Stroke" Value="#666666"/>
                                                <Setter Property="Fill" Value="#3A3A3A"/>
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding IsEnabled}" Value="True"/>
                                                            <Condition Binding="{Binding IsSelected}" Value="False"/>
                                                            <Condition Binding="{Binding IsTransmitting}" Value="False"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Stroke" Value="{StaticResource ConnectorNormal}"/>
                                                        <Setter Property="Fill" Value="#1A1A1A"/>
                                                    </MultiDataTrigger>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding IsEnabled}" Value="True"/>
                                                            <Condition Binding="{Binding IsSelected}" Value="True"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Stroke" Value="Orange"/>
                                                        <Setter Property="Fill" Value="Orange"/>
                                                    </MultiDataTrigger>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding IsEnabled}" Value="True"/>
                                                            <Condition Binding="{Binding IsTransmitting}" Value="True"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Stroke" Value="#5CB85C"/>
                                                        <Setter Property="Fill" Value="#5CB85C"/>
                                                        <Setter Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect Color="#5CB85C" BlurRadius="15" ShadowDepth="0" Opacity="1"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </MultiDataTrigger>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding IsEnabled}" Value="True"/>
                                                            <Condition Binding="{Binding HasActiveRoute}" Value="True"/>
                                                            <Condition Binding="{Binding IsSelected}" Value="False"/>
                                                            <Condition Binding="{Binding IsTransmitting}" Value="False"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Stroke" Value="{StaticResource ConnectorActive}"/>
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name, Mode=OneWay}" FontWeight="Medium" FontSize="12" 
                                                  Foreground="#E0E0E0" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock FontSize="10" Foreground="#777" Margin="0,3,0,0">
                                            <Run Text="{Binding TypeText, Mode=OneWay}"/>
                                        </TextBlock>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                </ScrollViewer>
                <Canvas x:Name="OutputDropCanvas" Panel.ZIndex="0" IsHitTestVisible="False"/>
            </Grid>

            <!-- 连线提示 -->
            <TextBlock Grid.Column="1" Text="点击左侧输入设备的连接点，然后点击右侧输出设备创建连线&#x0a;右键点击连线可删除"
                      Foreground="#444" FontSize="11" TextAlignment="Center"
                      HorizontalAlignment="Center" VerticalAlignment="Center"
                      TextWrapping="Wrap" IsHitTestVisible="False"
                      Panel.ZIndex="-1"/>
            
            <!-- NM2 发现设备面板 -->
            <Border Grid.Column="1" Background="#252525" CornerRadius="8" Padding="12"
                    VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,10"
                    Visibility="{Binding ShowNM2DevicesPanel, Converter={StaticResource BoolToVis}}"
                    Panel.ZIndex="2" MinWidth="220"
                    BorderBrush="#3D3D3D" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <TextBlock Text="发现的设备" FontWeight="SemiBold" 
                                   Foreground="#CCC" FontSize="11"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <Button Content="✕" Command="{Binding CloseDiscoveryPanelCommand}"
                                HorizontalAlignment="Right" VerticalAlignment="Center"
                                FontSize="10" Foreground="#666" Background="Transparent"
                                BorderThickness="0" Cursor="Hand" Padding="4,0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="#AAA"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </Grid>
                    <ItemsControl Grid.Row="1" ItemsSource="{Binding DiscoveredNM2Devices}" MaxHeight="150"
                                  HorizontalContentAlignment="Center">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#2D2D2D" CornerRadius="6" Padding="10,6" Margin="0,0,0,4"
                                        Cursor="Hand" MouseLeftButtonDown="NM2Device_Click"
                                        BorderBrush="#3D3D3D" BorderThickness="1" MinWidth="180">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#353535"/>
                                                    <Setter Property="BorderBrush" Value="#4A90D9"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding Name}" Foreground="#E0E0E0" FontSize="11"/>
                                            <TextBlock Foreground="#777" FontSize="9" Margin="0,2,0,0">
                                                <Run Text="{Binding Host}"/>:<Run Text="{Binding Port}"/>
                                            </TextBlock>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="连接" Foreground="#4A90D9" FontSize="10" VerticalAlignment="Center" FontWeight="Medium"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Border>
        </Grid>

        <!-- 底部状态栏 -->
        <StatusBar Grid.Row="3" Background="{StaticResource HeaderBg}" Padding="10,5" BorderBrush="#2A2A2A" BorderThickness="0,1,0,0">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="8" Height="8" Margin="0,0,8,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#555"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsNetworkRunning}" Value="True">
                                        <Setter Property="Fill" Value="#5CB85C"/>
                                        <Setter Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect Color="#5CB85C" BlurRadius="8" ShadowDepth="0" Opacity="0.8"/>
                                            </Setter.Value>
                                        </Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#AAA" FontSize="11" VerticalAlignment="Center"/>
                </StackPanel>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="路由: " Foreground="#666" FontSize="11" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding Routes.Count}" Foreground="#AAA" FontWeight="Medium" FontSize="11" VerticalAlignment="Center"/>
                    <TextBlock Text=" │ " Foreground="#333" FontSize="11" Margin="6,0" VerticalAlignment="Center"/>
                    <Button Content="清除所有路由" Command="{Binding ClearRoutesCommand}" Style="{StaticResource TransparentButton}" VerticalAlignment="Center"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>